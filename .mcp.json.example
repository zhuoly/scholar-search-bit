{
  "_comment": "复制此文件为 .mcp.json。args 路径改为 carsi-search-mcp/server.py 的实际绝对路径。Chrome 会自动启动无需手动配置。",
  "mcpServers": {
    "carsi-search": {
      "command": "python",
      "args": ["C:/Users/你的用户名/scholar-search/carsi-search-mcp/server.py"]
    }
  }
}
